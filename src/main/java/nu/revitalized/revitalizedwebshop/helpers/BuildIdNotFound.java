package nu.revitalized.revitalizedwebshop.helpers;

public class BuildIdNotFound {
    public static String buildIdNotFound(String title, Long id) {
        return title + " with id: " + id + " not found";
    }
}
